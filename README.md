# Attribute-Based-Encryption-Proxy-for-Databases-LGPD-Security-
A "gatekeeper" that sits between your application and the database. It analyzes SQL queries in real time and decides whether to decrypt sensitive data (such as CPF numbers or credit card details) based on who is making the request.
